// config.js — genereres automatisk av del.sh
// Ikke rediger manuelt — overskrives ved deling
window.BAVARIA_CONFIG = {
  backend: null,   // null = bruk localStorage eller localhost
  signalk: null,
};
